package Class;

public class outpackage {
    public static void main(String[] args) {
        // Book n = new Book(null, null, null, 0);  
        // BookStore M = new BookStore();

        // it's cannot declar because it's stay out package
    }
}
